/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: [
            'localhost',
            // Your domain(s) here
        ],
    },
}

module.exports = nextConfig
